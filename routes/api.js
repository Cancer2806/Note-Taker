// Required modules
const express = require('express');
const path = require('path');
const fs = require('fs');

const router = express.Router();

// Notes must have a unique id


// GET /api/notes should read the db.json file and return all saved notes



// POST /api/notes should add a new notes to the db.json file


// DELETE should remove the specific note







// module export
module.exports = router;